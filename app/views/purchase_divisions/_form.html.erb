<%= simple_form_for @purchase_division, :html => { :class => 'well form-horizontal' } do |f| %>
  <% if @purchase_division.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@purchase_division.errors.count, "error") %> prohibited this supplier_master from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @purchase_division.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>
  <%= f.input :purchase_division_name %>
  <%= f.input :purchase_division_long_name %>
  
  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                purchase_divisions_path, :class => 'btn btn-default' %>
<% end %>
